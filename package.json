{
  "dependencies": {
    "bootstrap": "^5.3.1",
    "concurrently": "^8.2.1",
    "nodemon": "^3.0.1",
    "react-bootstrap": "^2.8.0"
  },
  "scripts": {
    "start:server": "cd backend && npm install && node server.js",
    "start:client": "cd frontend &&  npm install && npm start",
    "start": "concurrently \"npm run start:server\" \"npm run start:client\""
  }
}
